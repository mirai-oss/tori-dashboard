# tori-dashboard — 作業前に必ず読むこと

飲食店グループ（12店舗）の経営ダッシュボード。素のJS（`app.js` 約6,000行）＋ GAS（`gas/Code.gs`）＋ スプレッドシートがDB。
公開: https://mirai-oss.github.io/tori-dashboard/ （mainへpushで自動デプロイ・1〜2分）

**このファイルは「守るルール」だけ。経緯・詳細は [HANDOFF.md](HANDOFF.md)、使い方は [README.md](README.md)。**

---

## 0. 最初の3分でやること（順番厳守）

```bash
cd /Users/mirai/Claude/tori-dashboard
git fetch origin && git status -sb   # ①並行セッションの有無を確認
bash scripts/status.sh               # ②本番の「実際の状態」を実測
```

そのうえで [HANDOFF.md](HANDOFF.md) の **「3. 未完了のタスク」** と **作業ログの最新1〜2件** を読む。

> ⚠️ **ドキュメントより実測を信じること。** ドキュメントは古い可能性があるが、`status.sh` が叩く ping / 公開ファイルは嘘をつかない。
> ⚠️ **ユーザーは別のClaudeセッションを並行で走らせる。** リモートが先行していたら**必ずrebaseで統合**（上書き厳禁・過去に複数回発生）。

---

## 1. 絶対にやってはいけないこと

| ❌ 禁止 | 理由（すべて実際に事故が起きている） |
|---|---|
| **`appsscript.json` の全文置き換え** | BigQueryの高度なサービス設定が消え、明細分析と毎朝の`dinii-orders`が全滅（2026-08-06）。**足りない項目だけ追記**する。→ [gas/appsscript_注意.md](gas/appsscript_注意.md) |
| **GASで「新しいデプロイ」を作る** | URLが変わり、`app.js`の`DEFAULT_API_URL`が旧版を指したまま事故る。正: **デプロイを管理 → 編集(鉛筆) → 新バージョン → デプロイ** |
| **`git push --force` / リモートを上書きするrebase** | 並行セッションの成果が消える |
| **`.github/workflows/` へのpush（要事前確認）** | 過去にトークンの`workflow`スコープ不足で拒否されたことがあるが、2026-08-22時点では`workflow`スコープ有りで普通にpushできた（`gh auth status`で確認）。**pushする前に`gh auth status`でスコープを確認**し、無ければ`scripts/lark-report.workflow.yml`を更新してユーザーにWeb UIで貼り替えてもらう。どちらの場合も`scripts/lark-report.workflow.yml`と`.github/workflows/lark-report.yml`は常に同じ内容に保つこと（乖離した実績あり） |
| **Webhook URL・トークン・パスワードをリポジトリに書く** | **Publicリポジトリ**。GitHub Secrets / ローカルのみ |
| **PL管理システムの`コード.gs`を上書き** | リポジトリに無い関数（`syncAd`等）が消える。追加は必ず**別ファイル**で |
| **ユーザーのChromeを勝手に操作** | 別プロジェクト(ns-daily-import)の取込が壊れる |

---

## 2. 変更したら必ずやること

| 変更したもの | 必須の付随作業 |
|---|---|
| `app.js` | **`index.html` の `?v=N` を +1**（上げないとブラウザが旧版を読み続け「直したのに直らない」で溶ける） |
| `gas/Code.gs` | **`ping` の `ver` を必ず上げる**（上げないと何が本番に出ているか外から判別不能になる。2026-08-06に実際に発生） |
| GASのデプロイ後 | `curl -sL '<GAS_URL>?action=ping'` で `ver` を確認 |
| 何か直した | `HANDOFF.md` の作業ログに追記 → commit → push |

**GAS変更はユーザーの手作業（貼り替え＋再デプロイ）が必要。手間をかけるので、必要な分をまとめて1回で済ませること。**
GASの全文を渡すときは、チャットに貼らず**ファイルで送る**（2,100行超・チャットでは切れる）。

---

## 3. 直す前に「再現・計測」する

今回（2026-08-06）「口座CSV取込が動かない」の真因は、**取込は5.6秒で成功しているのにUIが16秒間更新されない**というUI側の問題だった。コードを読むだけでは特定できず、本番で実測して初めて分かった。

- 症状を**再現してから**直す。推測で直さない
- 遅い・止まる系は**必ず時間を測る**（`performance.now()`でAPI往復を計測）
- **本番APIは安全に叩ける**: `?action=login&id=...&pw=...` でトークン取得 → `?action=data&token=...&keys=...`。
  書き込み系を試すときは**既存行と同一のデータ**を送れば重複判定でスキップされ、書き込みゼロで全経路を検証できる
- GASへのPOSTを`curl`で試すときは `--post302` を**付けない**（302でGETに変換されるのがブラウザと同じ挙動）

## 4. ローカル検証

```bash
python3 -m http.server 8766     # 8765は別セッションが専有しがち
```

ログインは本番APIを叩くので、デモで見たいときはコンソールで:

```js
apiUrl=()=>'';
S.auth={token:null,account:{id:'shacho',name:'社長',role:'社長',stores:'全店',tabs:'',perms:'',position:''}};
render();
```

`sample-data.js` の実売上は**2026-07-15頃まで**（それ以降は0円のプレースホルダ行）。0でもバグではない。

---

## 5. 設計上の約束（壊さない）

- **シート名が「DB_」で始まるものはGAS再デプロイ不要で自動配信**。新シートは`app.js`側の判定を足すだけで扱える
- 判定関数は `is○○Key()`。**前方一致に注意**（`週報回答`は`週報`にも一致するので厳密一致にしてある）
- **ドメインごとの正(source of truth)は1箇所**: 売上=分析_日別店舗 / 明細=BigQuery / 広告費=管理シート💾広告費DB / 経費=PL管理システム / 日報=Supabase。機能を足すときは正へ書き戻す
- 権限は3層（`role`権限 / `position`役職 / `stores`担当店舗）。優先順位は アカウント個別 → DB_権限定義の役職行 → 同権限行 → コード既定
- 「使える機能」の**空欄＝上位の既定に従う / `なし`＝明示的に全部不可** の区別は重要（消さない）
- モーダルの保存・取込成功時は **`S.modal=null` の直後に `render()`** して先に閉じる。重い再取得を`await`してから閉じると固まって見える（7箇所で発生済み）

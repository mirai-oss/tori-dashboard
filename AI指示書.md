# AIに作業を頼むときの指示書（コピペ用）

別のMacBookや、Claude Codeの別スレッドで作業を頼むとき、**下の枠の中をそのままコピーして貼る**だけでOK。
最後の「今回やってほしいこと」だけ書き換えてください。

---

## 【標準】いつもの1台で頼むとき

```text
/Users/mirai/Claude/tori-dashboard で作業してください。

■ 始める前に必ず（順番厳守）
1. cd /Users/mirai/Claude/tori-dashboard
2. cat CLAUDE.md を読む（このプロジェクトの禁止事項と必須ルール）
3. bash scripts/status.sh を実行して現状を実測する
   → ⚠️が出たら、その内容を私に報告してから作業を始めること
4. HANDOFF.md の「3. 未完了のタスク」と作業ログの最新2件を読む

■ 絶対にやらないこと
- appsscript.json の全文置き換え（BigQueryが消えて明細分析と毎朝の取込が全滅する）
- GASで「新しいデプロイ」を作る（URLが変わって事故る。正しくは デプロイを管理→編集→新バージョン）
- git push --force / リモートを上書きするrebase（別セッションの成果が消える）
- .github/workflows/ へのpush（トークンにスコープが無い）
- Webhook URL・トークン・パスワードをリポジトリに書く（Publicリポジトリ）
- PL管理システムの「コード.gs」を上書き（リポジトリに無い関数が消える。追加は別ファイルで）
- 私のChromeを勝手に操作しない

■ 守ること
- 私は別のClaudeセッションを同時に走らせることがある。push前に必ず git fetch し、
  リモートが先行していたらrebaseで統合すること（上書き厳禁）
- app.js を変えたら index.html の ?v= を必ず+1する
- gas/Code.gs を変えたら ping の ver を必ず上げる
- GASの変更は私の手作業（貼り替え＋再デプロイ）が必要。まとめて1回で済むようにして、
  全文を渡すときはチャットに貼らずファイルで送ること（2,100行超あるため）
- 不具合は推測で直さず、必ず再現・計測してから直すこと
- 作業したら HANDOFF.md の作業ログに追記して commit・push すること

■ 今回やってほしいこと
（ここに依頼内容を書く）
```

---

## 【初回】新しいMacBookで初めて頼むとき

```text
まず作業環境を用意してください。

1. git clone https://github.com/mirai-oss/tori-dashboard.git
2. cd tori-dashboard
3. cat CLAUDE.md を読む（このプロジェクトの禁止事項と必須ルール）
4. bash scripts/status.sh を実行して現状を実測する
   → ⚠️が出たら、その内容を私に報告してから作業を始めること
5. HANDOFF.md の「3. 未完了のタスク」と作業ログの最新2件を読む

■ 絶対にやらないこと
- appsscript.json の全文置き換え（BigQueryが消えて明細分析と毎朝の取込が全滅する）
- GASで「新しいデプロイ」を作る（URLが変わって事故る。正しくは デプロイを管理→編集→新バージョン）
- git push --force / リモートを上書きするrebase（別セッションの成果が消える）
- .github/workflows/ へのpush（トークンにスコープが無い）
- Webhook URL・トークン・パスワードをリポジトリに書く（Publicリポジトリ）
- 私のChromeを勝手に操作しない

■ 守ること
- 私は別のClaudeセッションを同時に走らせることがある。push前に必ず git fetch し、
  リモートが先行していたらrebaseで統合すること（上書き厳禁）
- app.js を変えたら index.html の ?v= を必ず+1する
- gas/Code.gs を変えたら ping の ver を必ず上げる
- 不具合は推測で直さず、必ず再現・計測してから直すこと
- 作業したら HANDOFF.md の作業ログに追記して commit・push すること

※このMacBookには gh コマンドの認証が無いかもしれません。pushで認証を求められたら私に教えてください。

■ 今回やってほしいこと
（ここに依頼内容を書く）
```

---

## 【短縮版】ちょっとした調べもの・質問だけのとき

書き込みを伴わない相談なら、これだけで足ります。

```text
/Users/mirai/Claude/tori-dashboard で作業してください。
まず CLAUDE.md を読んで、bash scripts/status.sh で現状を確認してから答えてください。
コードやGAS、シートは私の許可なく変更しないでください。

■ 聞きたいこと
（ここに質問を書く）
```

---

## 補足：なぜこの手順なのか

| 手順 | 防いでいる事故 |
|---|---|
| `git fetch` してから作業 | 並行セッションの成果を上書きしてしまう（2026-08-06に実際に発生） |
| `status.sh` を先に実行 | ドキュメントが古いまま作業を始めてしまう。GASの未デプロイ・Pagesの未反映を見落とす |
| appsscript.json の全文置換禁止 | BigQueryの高度なサービス設定が消え、明細分析と毎朝のdinii-ordersが全滅（同日発生） |
| `?v=` と `ping ver` を上げる | 「直したのに直らない」／本番に何が出ているか判別不能になる（同日発生） |
| 再現・計測してから直す | 「取込が動かない」の真因が「取込は成功しUIが16秒固まっていただけ」だった（同日発生） |

**このリポジトリには `CLAUDE.md` があり、Claude Codeはそれを自動で読み込みます。**
上の指示書は、それが確実に読まれるよう念押しするためのものです（別マシンでcloneし忘れた場合の保険も兼ねています）。

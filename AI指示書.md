# AIに作業を頼むときの指示書（コピペ用）

別のMacBookや、Claude Codeの別スレッドで作業を頼むとき、**下の枠の中をそのままコピーして貼る**だけでOK。
「今回やってほしいこと」だけ書き換えてください。

> ⚠️ **絶対パスは書かないこと。** MacBookごとにパスが違います（`/Users/mirai/Claude/...` と `/Users/shunjjnakayama/...` など）。
> 下の指示書は**リポジトリ名で指定して自動で探させる**方式にしてあるので、どのマシンでもそのまま使えます。

---

## まず「どのシステムの話か」を決める

依頼内容によって触るリポジトリが違います。**間違えると別システムを壊します。**

| やりたいこと | リポジトリ | 中身 |
|---|---|---|
| 売上・PL・広告・入金・週報・口コミ・着地予測 | **tori-dashboard** | 経営ダッシュボード（素のJS＋GAS＋スプレッドシート） |
| 日報・週報フォーマット・提出通知・応募者一覧 | **nippo** | 日報サイト（Supabase＋index.html） |
| 共通ログイン・ポータル・権限設定 | **ns-portal** | 統合ポータル（Supabase） |
| 社内情報・書類・口座・従業員マスタ | **ns-info-system** | 社内情報管理（Next.js＋Supabase＋Vercel） |
| 業務委託の精算書 | **seisan-dashboard** | 精算ダッシュボード（GAS） |

すべて `https://github.com/mirai-oss/<リポジトリ名>` にあります。

---

## 【標準】作業を頼むとき

```text
リポジトリ「<ここにリポジトリ名>」で作業してください。

■ 始める前に必ず（順番厳守）
1. このパソコンのどこにあるか探す:
   ls -d ~/<リポジトリ名> ~/Claude/<リポジトリ名> 2>/dev/null
   見つからなければ: find ~ -maxdepth 4 -type d -name "<リポジトリ名>" 2>/dev/null
   それでも無ければ: git clone https://github.com/mirai-oss/<リポジトリ名>.git
2. そのディレクトリに cd して git fetch origin && git status -sb
   → リモートが先行していたら rebase で統合する（上書き厳禁）
   → 古いclone だと最新のルールやスクリプトが入っていないので必ず先に同期する
3. CLAUDE.md があれば読む（禁止事項と必須ルール）
4. scripts/status.sh があれば bash で実行して現状を実測する
   → ⚠️が出たら、その内容を私に報告してから作業を始めること
5. HANDOFF.md か README.md の未完了タスク・作業ログの最新2件を読む

■ 依頼内容がそのリポジトリの担当かどうか、先に確認すること
私が依頼先を間違えることがあります。読んでみて「これは別システムの話だ」と思ったら、
作業を始める前に指摘してください。

■ 絶対にやらないこと
- git push --force / リモートを上書きするrebase（別セッションの成果が消える）
- 私が明示的に頼んでいない範囲の変更
- Webhook URL・トークン・パスワードをリポジトリに書く（Publicリポジトリです）
- 私のChromeを勝手に操作しない
- 【GASを使うシステム(tori-dashboard / seisan-dashboard)の場合】
  ・appsscript.json の全文置き換え（BigQueryが消えて明細分析と毎朝の取込が全滅する）
  ・「新しいデプロイ」を作る（URLが変わって事故る。正しくは デプロイを管理→編集→新バージョン）
  ・PL管理システムの「コード.gs」を上書き（リポジトリに無い関数が消える。追加は別ファイルで）

■ 守ること
- 私は別のClaudeセッションを複数のパソコンで同時に走らせることがある。
  push前に必ず git fetch し、リモートが先行していたらrebaseで統合すること（上書き厳禁）
- 不具合は推測で直さず、必ず再現・計測してから直すこと
- 作業したら HANDOFF.md（無ければREADME.md）の作業ログに追記して commit・push すること
- 【tori-dashboard】app.js を変えたら index.html の ?v= を必ず+1する
- 【tori-dashboard / seisan-dashboard】GASコードを変えたら ping の ver を必ず上げる。
  GASの変更は私の手作業（貼り替え＋再デプロイ）が必要なのでまとめて1回で済むようにし、
  全文を渡すときはチャットに貼らずファイルで送ること

■ 今回やってほしいこと
（ここに依頼内容を書く）
```

---

## 【短縮版】調べもの・質問だけのとき

```text
リポジトリ「<ここにリポジトリ名>」について教えてください。
このパソコンでの場所を探して（ls -d ~/<名前> ~/Claude/<名前>）、git fetch して最新にしてから、
CLAUDE.md と HANDOFF.md を読んで答えてください。
コード・GAS・シートは私の許可なく変更しないでください。

■ 聞きたいこと
（ここに質問を書く）
```

---

## 補足：なぜこの手順なのか

| 手順 | 防いでいる事故 |
|---|---|
| リポジトリ名で探させる（絶対パスを書かない） | MacBookごとにパスが違い「そのディレクトリは存在しません」で止まる（2026-08-06に実際に発生） |
| 最初に `git fetch` して同期 | 古いcloneのまま作業を始め、最新のルール・スクリプトが無いまま進む／並行セッションの成果を上書きする（同日発生） |
| 依頼先が合っているか確認させる | 依頼を投げるリポジトリを間違える（同日発生。日報の依頼を売上ダッシュボードに投げた） |
| `status.sh` を先に実行 | GASの未デプロイ・Pagesの未反映を見落とす |
| appsscript.json の全文置換禁止 | BigQueryの設定が消え、明細分析と毎朝のdinii-ordersが全滅（同日発生） |
| `?v=` と `ping ver` を上げる | 「直したのに直らない」／本番に何が出ているか判別不能になる（同日発生） |
| 再現・計測してから直す | 「取込が動かない」の真因が「取込は成功しUIが16秒固まっていただけ」だった（同日発生） |

**tori-dashboard には `CLAUDE.md` があり、Claude Codeはそれを自動で読み込みます**（ただしcloneが最新である必要あり）。
上の指示書は、それが読まれない環境でも最低限の安全が確保されるよう、重要なルールを本文にも書いてあります。
